import { InputDropdownDirective } from './input-dropdown.directive';

describe('InputDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new InputDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
